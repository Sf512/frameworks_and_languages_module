Server

The completed server contains : app.js, datamodel.js, Dockerfile, Makefile and the package.json.
The command 'make run_local' will get the server started up once in the server directory using 'cd server'
If at any point the server needs to be stopped then 'C^' is what needs to be entered. 



-curl commands used to add
-bugging debugging to add
-what each file contains 
-comments

